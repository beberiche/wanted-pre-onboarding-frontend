import { BrowserRouter } from 'react-router-dom';

/**
 * @description
 * React-Router 경로 설정을 관리하는 래퍼 컴포넌트
 *
 * @author bell
 */
const RouterWrapper = () => {
  return <BrowserRouter />;
};

export default RouterWrapper;
